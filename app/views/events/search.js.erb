$('.search-results').remove();
$('#search').append('<%= j render("results") %>');
$('.search-result').on('click', function(){
	var selection = $(this);
	selectThisEvent(selection);
	$(this).closest('.search-results').remove();

});
