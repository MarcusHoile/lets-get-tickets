<!DOCTYPE html>
<html>
<head>
  <%= yield :title %>
  <%= include_gon %>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <%= render "layouts/googleanalytics" %>
  <%= yield :head %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <%= render "layouts/header" %>
  
  <div class="row alerts">
    <div class="col-md-12">
      <% flash.each do |key, value| %>
        <%= content_tag :div, value, :class => "alert alert-#{key}" unless value.blank? %>
      <% end %>
    </div>
  </div>
  
  <%= yield :event_notice  %>

    <div class="container">
      <%= yield %>
    </div>
  <div class="feedback">
    <div class="feedback-tab">feedback</div>
    <div class="feedback-form">
      <p>We'd love to hear what you think about our site and help make it better. Tell us about your experience.</p>
    </div>
  </div>
  <script>
    $('.feedback-tab').on('click', function(){
      $('.feedback').toggleClass('form-open');
    })
  </script>
  <footer>
    <ul class="footer">
      <li><%= link_to 'About Us', '/about' %></li>
      <li><%= link_to 'Contact Us', '/contact' %></li>
    </ul>
  </footer>
</body>

</html>
