$('.rsvp-btn').removeClass('active');
$('.rsvp-btn.<%= @invite.rsvp %>').addClass('active');

var $inviteRsvp = $('#invite-<%= @invite.id %>');
$('#invite-<%= @invite.id %> span').removeClass('maybe going not-going').addClass('<%= @invite.rsvp %>');

<% if @invite.rsvp == "going" && @current_rsvp != "going" %>
	$('#paymentModal').modal('show');
<% end %>


